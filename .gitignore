# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — jangan pernah commit rahasia (rahasia di .env.*)
# Varian ini bisa dibuat oleh Next.js / Vercel / CI saat deploy:
#   .env, .env.local, .env.development, .env.production, .env.test,
#   .env.staging, .env.*.local — semuanya dikecualikan.
.env
.env.*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# data runtime (mock db dibuat otomatis saat pertama jalan)
/data/db.json
/data/*.tmp

# agent workspace (Freebuff) & file scratch debug
.freebuff/
cj*.txt

# supabase CLI local state
supabase/.branches
supabase/.temp
